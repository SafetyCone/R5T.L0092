using System;


namespace R5T.L0092.T001
{
	/// <summary>
	/// Basic general check types library.
	/// </summary>
	public static class Documentation
	{
        ///// <summary>
        ///// Note that the Windows directory separator is the standard directory separator, so a path using the standard directory separator is a Windows path.
        ///// This is expressed by <see cref="IUsesStandardDirectorySeparator"/> inheriting from <see cref="IIsWindowsPath"/>.
        ///// This is true even if the path does not actually contain a directory separator!
        ///// While the two are synonymous, inheritance needs a direction. Because 
        ///// </summary>
        //public static readonly object StandardSeparatorPathsAreWindowsPaths;
	}
}
